# CLAUDE.md

## API folder

- Never modify files in the `API` folder directly.
- The API functions and types are generated. To refresh them, run:

  ```sh
  npm run generate-api
  ```

- Run this periodically to keep the generated API functions and types up to date.
